global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'ems'

scrape_configs:
  - job_name: 'ems-backend'
    scrape_interval: 10s
    metrics_path: /management/prometheus
    static_configs:
      - targets:
          - app:8080
